#!/bin/bash

# Get the directory where this script is located
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"

# Run the voice-enabled Claude Code interactive mode
"$SCRIPT_DIR/claude-voice-env/bin/python" "$SCRIPT_DIR/claude_code_voice_integration.py" "$@"